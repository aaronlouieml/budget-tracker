DATABASE_URL=postgres://budget_tracker:changeme@localhost:5432/budget_tracker
PORT=3000
JWT_SECRET=changeme

# Receipt scanning (OCR). Provider is pluggable via OCR_PROVIDER.
# Currently supported: "ocrspace" (free tier available at https://ocr.space/ocrapi)
OCR_PROVIDER=ocrspace
OCR_SPACE_API_KEY=changeme
