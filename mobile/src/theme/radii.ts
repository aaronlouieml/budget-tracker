// One corner-radius scale for the whole app.
export const radii = {
  control: 12, // chips, small buttons, inputs
  card: 18, // standard cards/surfaces
  cardLarge: 24, // hero/feature cards (e.g. the Home balance card)
  button: 14,
  pill: 999,
} as const;
